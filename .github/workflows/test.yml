name: Tests

on:
  push:
    branches:
    - develop
  pull_request:

jobs:
  acceptance:
    name: Acceptance Tests
    runs-on: ubuntu-latest

    strategy:
      matrix:
        rmq_version: ["3.8", "3.9", "3.10", "3.11", "3.12", "3.13", "4.0"]

    env:
      RABBITMQ_VERSION: ${{ matrix.rmq_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Tests
        id: test
        run: make testacc
        continue-on-error: true  # Allows this step to fail without failing the job

      - name: Report results
        if: ${{ steps.test.outcome == 'success' }}
        run: echo "Tests passed for RabbitMQ v${{ matrix.node }}."

      - name: Handle failure
        if: ${{ steps.test.outcome == 'failure' }}
        run: echo "Tests failed for RabbitMQ v${{ matrix.node }}."
        